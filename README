For this setup you need:

* Backtrack in a VirtualBox with vagrant (use vagrant to provision)
* Long range USB wifi adapter (set VB USB filter)

/opt/metasploit/config/database.yml

# Setup
  ln -s /vagrant $HOME/v

# Wifi Usage
* Connect devices: left, right
  v/wifup
  v/cells

* Connect the network:
  v/wpa
  ctrl + z
  bg; v/wpa dhcp; fg

* Start metasploit:
  v/hermitage

